// Polyfills for older browsers
// This file provides compatibility for features that may not be available in all browsers

// Promise polyfill (if needed)
if (typeof Promise === "undefined") {
  console.warn("Promise is not supported in this environment");
}

// Minimal polyfill - add more as needed
(function() {
  'use strict';
  
  // You can add specific polyfills here if needed for older browsers
  
})();
